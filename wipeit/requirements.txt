# Core dependencies
cryptography>=41.0.0
psutil>=5.9.0
pyserial>=3.5

# Certificate generation
reportlab>=4.0.0
qrcode>=7.4.0
Pillow>=10.0.0

# GUI framework (optional - choose one)
PyQt6>=6.5.0
# OR tkinter (built-in to Python)

# CLI tools
click>=8.1.0
colorama>=0.4.6

# Development and testing
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.0.0
flake8>=6.0.0

# Documentation
sphinx>=7.1.0
sphinx-rtd-theme>=1.3.0

# Platform-specific dependencies
# Windows WMI
pywin32>=306; sys_platform == "win32"
wmi>=1.5.1; sys_platform == "win32"

# Linux utilities (installed via package manager)
# hdparm, nvme-cli, sgdisk, smartmontools

# Android utilities (ADB)
ppadb>=0.3.0
